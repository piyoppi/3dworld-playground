//export const flatChildItem = (coordinate: Coordinate): Array<Item> => {
//  return [
//    ...coordinate.items,
//    ...coordinate.children.map(childCoordinate => flatChildItem(childCoordinate)).flat()
//  ]
//}
